"use client";
import {useState} from "react";import {supabaseBrowser} from "../../lib/supabase-browser";import Link from "next/link";
export default function ResetPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[mode,setMode]=useState<"request"|"update">("request");const[msg,setMsg]=useState("");const[error,setError]=useState("");
async function request(e:React.FormEvent){e.preventDefault();setError("");setMsg("");const redirect=window.location.origin+"/cph/dat-lai-mat-khau";const{error}=await supabaseBrowser.auth.resetPasswordForEmail(email,{redirectTo:redirect});if(error)setError(error.message);else setMsg("Đã gửi email đặt lại mật khẩu. Hãy kiểm tra hộp thư.")}
async function update(e:React.FormEvent){e.preventDefault();setError("");setMsg("");const{error}=await supabaseBrowser.auth.updateUser({password});if(error)setError(error.message);else{setMsg("Đổi mật khẩu thành công. Bạn có thể đăng nhập lại.");setMode("request");setPassword("")}}
return <main className="min-h-screen bg-slate-50"><section className="container-page flex min-h-screen max-w-lg items-center py-10"><div className="card w-full p-6 sm:p-8"><Link href="/" className="text-sm font-bold text-brand-700">← Phú Thọ Market</Link><h1 className="mt-5 text-2xl font-black">Đặt lại mật khẩu</h1>{mode==="request"?<form onSubmit={request} className="mt-6 space-y-3"><p className="text-sm leading-6 text-slate-500">Nhập email đã đăng ký, chúng tôi sẽ gửi liên kết đặt lại mật khẩu.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl border px-4 py-3"/><button className="w-full rounded-xl bg-brand-600 py-3 font-extrabold text-white">Gửi liên kết</button></form>:<form onSubmit={update} className="mt-6 space-y-3"><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mật khẩu mới" className="w-full rounded-xl border px-4 py-3"/><button className="w-full rounded-xl bg-brand-600 py-3 font-extrabold text-white">Đổi mật khẩu</button></form>}{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}{msg&&<p className="mt-4 rounded-xl bg-brand-50 p-3 text-sm text-brand-700">{msg}</p>}<button type="button" onClick={()=>setMode(mode==="request"?"update":"request")} className="mt-5 text-sm font-bold text-slate-500">{mode==="request"?"Tôi đã mở liên kết đặt lại mật khẩu":"Quay lại gửi email"}</button></div></section></main>}