"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import Header from "../../components/Header";import AuthGuard from "../../components/AuthGuard";import {supabaseBrowser} from "../../lib/supabase-browser";
export default function EditListingPage(){const router=useRouter();const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState("");const[id,setId]=useState("");const[cats,setCats]=useState<any[]>([]);const[locs,setLocs]=useState<any[]>([]);const[form,setForm]=useState({title:"",description:"",price:"",category_id:"",location_id:"",condition:"used",phone:""});
useEffect(()=>{const listingId=new URLSearchParams(window.location.search).get("id")||"";setId(listingId);(async()=>{const{data:{user}}=await supabaseBrowser.auth.getUser();if(!user||!listingId){setLoading(false);return}const[c,l,x]=await Promise.all([supabaseBrowser.from("categories").select("id,name").order("name"),supabaseBrowser.from("locations").select("id,name").eq("level","area").order("name"),supabaseBrowser.from("listings").select("id,title,description,price,category_id,location_id,condition,phone").eq("id",listingId).eq("seller_id",user.id).maybeSingle()]);setCats(c.data||[]);setLocs(l.data||[]);if(x.data)setForm({title:x.data.title||"",description:x.data.description||"",price:String(x.data.price||""),category_id:x.data.category_id||"",location_id:x.data.location_id||"",condition:x.data.condition||"used",phone:x.data.phone||""});else setError("Không tìm thấy tin hoặc bạn không có quyền sửa tin.");setLoading(false)})()},[]);
async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError("");const{error}=await supabaseBrowser.from("listings").update({title:form.title,description:form.description,price:Number(form.price)||0,category_id:form.category_id,location_id:form.location_id,condition:form.condition,phone:form.phone,status:"pending",rejection_reason:null}).eq("id",id);if(error)setError(error.message);else router.push("/tai-khoan");setSaving(false)}
return <main><Header/><section className="container-page py-10"><AuthGuard><Link href="/tai-khoan" className="text-sm font-bold text-brand-700">← Quản lý tin</Link><h1 className="mt-3 text-3xl font-black">Sửa tin đăng</h1>{loading?<div className="py-10 text-slate-500">Đang tải...</div>:!id?<div className="card mt-6 p-6 text-red-600">{error||"Thiếu mã tin."}</div>:<form onSubmit={save} className="card mt-6 p-6"><div className="grid gap-5 md:grid-cols-2"><label className="md:col-span-2"><span className="mb-2 block text-sm font-bold">Tiêu đề</span><input required maxLength={140} value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="w-full rounded-xl border px-4 py-3"/></label><label><span className="mb-2 block text-sm font-bold">Danh mục</span><select required value={form.category_id} onChange={e=>setForm({...form,category_id:e.target.value})} className="w-full rounded-xl border bg-white px-4 py-3">{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-bold">Khu vực</span><select required value={form.location_id} onChange={e=>setForm({...form,location_id:e.target.value})} className="w-full rounded-xl border bg-white px-4 py-3">{locs.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-bold">Giá bán (VNĐ)</span><input inputMode="numeric" value={form.price} onChange={e=>setForm({...form,price:e.target.value.replace(/\D/g,"")})} className="w-full rounded-xl border px-4 py-3"/></label><label><span className="mb-2 block text-sm font-bold">Tình trạng</span><select value={form.condition} onChange={e=>setForm({...form,condition:e.target.value})} className="w-full rounded-xl border bg-white px-4 py-3"><option value="like_new">Như mới</option><option value="used">Đã sử dụng</option><option value="new">Mới</option></select></label><label className="md:col-span-2"><span className="mb-2 block text-sm font-bold">Số điện thoại</span><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} className="w-full rounded-xl border px-4 py-3"/></label><label className="md:col-span-2"><span className="mb-2 block text-sm font-bold">Mô tả</span><textarea required minLength={10} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="min-h-40 w-full rounded-xl border px-4 py-3"/></label></div>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}<p className="mt-4 text-xs text-slate-500">Sau khi sửa, tin sẽ quay lại trạng thái chờ duyệt để kiểm tra nội dung mới.</p><div className="mt-5 flex gap-2"><button disabled={saving} className="rounded-xl bg-brand-600 px-6 py-3 font-extrabold text-white">{saving?"Đang lưu...":"Lưu & gửi duyệt"}</button><Link href="/tai-khoan" className="rounded-xl border px-6 py-3 font-bold">Hủy</Link></div></form>}</AuthGuard></section></main>}