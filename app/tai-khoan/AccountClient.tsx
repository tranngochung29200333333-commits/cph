"use client";import {useEffect,useState} from "react";import Link from "next/link";import {supabaseBrowser} from "../../lib/supabase-browser";import AuthGuard from "../../components/AuthGuard";
export default function AccountClient(){const[items,setItems]=useState<any[]>([]);const[user,setUser]=useState<any>(null);const[loading,setLoading]=useState(true);useEffect(()=>{supabaseBrowser.auth.getUser().then(async({data})=>{setUser(data.user);if(data.user){const{data:rows}=await supabaseBrowser.from("listings").select("id,title,price,status,created_at").eq("seller_id",data.user.id).order("created_at",{ascending:false});setItems(rows||[])}setLoading(false)})},[]);async function signout(){await supabaseBrowser.auth.signOut();location.href="/"}return <AuthGuard><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-extrabold uppercase tracking-widest text-brand-600">Tài khoản</p><h1 className="mt-1 text-3xl font-black">Quản lý tin của tôi</h1><p className="mt-2 text-slate-500">{user?.email}</p></div><div className="flex gap-2"><Link href="/dang-tin" className="rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-extrabold text-white">+ Đăng tin</Link><button onClick={signout} className="rounded-xl border bg-white px-4 py-2.5 text-sm font-bold">Đăng xuất</button></div></div><div className="card mt-7 overflow-hidden">{loading?<div className="p-8">Đang tải...</div>:!items.length?<div className="p-10 text-center text-slate-500">Bạn chưa có tin đăng.</div>:<div className="divide-y">{items.map(x=><div key={x.id} className="flex flex-wrap items-center justify-between gap-3 p-5"><div><h2 className="font-extrabold">{x.title}</h2><p className="mt-1 text-sm text-brand-700">{new Intl.NumberFormat("vi-VN").format(x.price)} đ</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{x.status==="published"?"Đang hiển thị":x.status==="pending"?"Chờ duyệt":x.status==="sold"?"Đã bán":"Từ chối"}</span></div>)}</div>}</div></AuthGuard>}
