"use client";
import Link from "next/link";import Header from "../../components/Header";import {useEffect,useState} from "react";import {supabaseBrowser} from "../../lib/supabase-browser";
export default function SearchPage(){const[q,setQ]=useState("");const[items,setItems]=useState<any[]>([]);useEffect(()=>{const term=new URLSearchParams(window.location.search).get("q")||"";setQ(term);if(!term)return;supabaseBrowser.from("listings").select("id,title,price,images,condition").eq("status","published").ilike("title","%"+term+"%").order("created_at",{ascending:false}).limit(30).then(({data})=>setItems(data||[]));},[]);return <main><Header/><section className="container-page py-10"><h1 className="text-3xl font-black">Kết quả tìm kiếm</h1><p className="mt-2 text-slate-500">Từ khóa: <b>{q||"Tất cả"}</b></p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(x=><Link href={"/tin?id="+x.id} key={x.id} className="group overflow-hidden rounded-2xl border bg-white"><img src={x.images?.[0]} className="aspect-[4/3] w-full object-cover group-hover:scale-105" alt=""/><div className="p-4"><h2 className="line-clamp-2 text-sm font-bold">{x.title}</h2><p className="mt-2 font-black text-brand-700">{new Intl.NumberFormat("vi-VN").format(x.price)} đ</p></div></Link>)}</div>{q&&!items.length&&<div className="card mt-6 p-10 text-center text-slate-500">Chưa có tin phù hợp. Hãy thử từ khóa khác.</div>}</section></main>}