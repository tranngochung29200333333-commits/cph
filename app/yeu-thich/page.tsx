"use client";
import {useEffect,useState} from "react";import Link from "next/link";import Header from "../../components/Header";import AuthGuard from "../../components/AuthGuard";import {Heart} from "lucide-react";import {supabaseBrowser} from "../../lib/supabase-browser";
const money=new Intl.NumberFormat("vi-VN");
export default function FavoritesPage(){const[items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true);
 async function load(){const{data:{user}}=await supabaseBrowser.auth.getUser();if(!user){setLoading(false);return}const{data}=await supabaseBrowser.from("favorites").select("listing_id,listings(id,title,price,images,condition,status,expires_at,locations(name))").eq("user_id",user.id).order("created_at",{ascending:false});setItems((data||[]).map((x:any)=>x.listings).filter((x:any)=>x && (!x.expires_at || new Date(x.expires_at)>new Date()) && x.status==="published"));setLoading(false)}
 useEffect(()=>{load()},[]);
 return <AuthGuard><Header/><section className="container-page py-8"><div className="flex items-end justify-between"><div><p className="text-sm font-extrabold uppercase tracking-widest text-brand-600">Cá nhân</p><h1 className="mt-1 text-3xl font-black">Tin đã yêu thích</h1></div><Heart className="text-red-500" fill="currentColor"/></div>{loading?<div className="py-12 text-center text-slate-500">Đang tải...</div>:!items.length?<div className="card mt-7 p-10 text-center text-slate-500">Bạn chưa lưu tin nào. Hãy bấm ❤️ ở trang chi tiết tin.</div>:<div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(x=><Link key={x.id} href={"/tin?id="+x.id} className="group overflow-hidden rounded-2xl border bg-white hover:shadow-soft"><div className="aspect-[4/3] overflow-hidden bg-slate-100">{x.images?.[0]&&<img src={x.images[0]} alt="" className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/>}</div><div className="p-4"><h2 className="line-clamp-2 min-h-10 text-sm font-bold">{x.title}</h2><p className="mt-2 font-black text-brand-700">{money.format(x.price)} đ</p><p className="mt-2 text-xs text-slate-500">📍 {x.locations?.name||"Phú Thọ"}</p></div></Link>)}</div>}</section></AuthGuard>}
